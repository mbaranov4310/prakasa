# uninflected-upasarga

- **Judgment** — Match each of the twenty printed upasargas to its gloss, and form `prati` with a case-2 noun (“regarding …”).
- **Outcome** — `generate`
- **What I would need** — nothing for the list or `prati` + case 2; verb-meaning shifts and independent `api` sentences stay as lesson prose (no productive rule / no sentence stock).
- **What I did** — Replaced the five-card stub with **25** cards. Groups: `upasarga|list` (20 distinct IAST from the lesson list); `prati|case2` (`rāmaṃ`, `tat`, swapped `aśva` / `gaja` / `nara`). Cue: First Steps Towards Sanskrit §5.1 (book p.97) defers prefixes — no list exercise; inventory shape like §3.6 (book p.50); cells from the lesson. Devanagari checked with `scripts/rudra-iast.py`.
- **Check** — `unverified`
