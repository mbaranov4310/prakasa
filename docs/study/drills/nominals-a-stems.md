# nominals-a-stems

**Judgment** — This page drills masculine short `-a` case endings from the printed 8×3 table (and the n→ṇ note), so a learner can pick the right form for a given case, number, and stem.

**Outcome** — generate

**What I would need** — nothing

**What I did** — 42 cards on stems `rāma`, `aśva`, and `gaja` (singular and plural only). Groups: `rāma|sg`, `rāma|pl`, `aśva|sg`, `aśva|pl`, `gaja|sg`, `gaja|pl` (each ≥4 distinct IAST). Collapsed shared spellings (pl nom=voc; pl dat=abl). Dual left out: only three distinct spellings after collapse. Cue: First Steps Towards Sanskrit §6.3 `deva` paradigm (p.121) and §6.7.1 (p.131); endings from the lesson `rāma` table; `aśva`/`gaja` swapped in. Devanagari checked with `scripts/rudra-iast.py`.

**Check** — unverified
