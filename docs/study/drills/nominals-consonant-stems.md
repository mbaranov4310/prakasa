# nominals-consonant-stems

**Judgment** — This page drills consonant-stem case forms (basic and strong/weak) from the printed paradigms.

**Outcome** — generate

**What I would need** — nothing

**What I did** — Replaced the thin five-card deck with **37** cards from printed lesson cells only: stems `marut` (basic), `bhagavat` (-at), `ātman` (-an); singular and plural. Groups: `marut|sg`, `marut|pl`, `bhagavat|sg`, `bhagavat|pl`, `ātman|sg`, `ātman|pl` (each ≥4 distinct IAST). Collapsed shared spellings (e.g. sg abl=gen; marut pl nom=acc=voc; pl dat=abl). Dual left out (only three distinct spellings after collapse). No noun swaps — lesson does not print a second stem of the same join. Cue: Gonda §33 marut (p.26) and §36–39 -at/-an shape (pp.29–31). Devanagari checked with `scripts/rudra-iast.py`.

**Check** — unverified
