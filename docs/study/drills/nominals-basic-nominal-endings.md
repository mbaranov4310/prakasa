# nominals-basic-nominal-endings

**Judgment** — This page drills the basic ending set through the printed feminine `nau` and neuter `manas` paradigms (case × number forms of one stem).

**Outcome** — generate

**What I would need** — nothing

**What I did** — 21 cards on stems `nau` and `manas` (singular and plural only). Groups: `nau|sg`, `nau|pl`, `manas|sg`, `manas|pl` (each ≥4 distinct IAST). Collapsed shared spellings (e.g. sg abl=gen; pl nom=acc=voc; pl dat=abl). Dual left out: only three distinct spellings after collapse. Cue: First Steps Towards Sanskrit §6.3 paradigm (p.121) and §6.7.1 (p.131); forms from the lesson tables only (irregular stems — no invented nouns). Devanagari checked with `scripts/rudra-iast.py`.

**Check** — unverified
