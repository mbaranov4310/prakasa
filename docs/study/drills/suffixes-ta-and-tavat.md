# suffixes-ta-and-tavat

**Judgment** — Form past participles with `-ta` (PPP/kta) and `-tavat` (active) using the same root sound changes as `-tvā`.

**Outcome** — generate

**What I would need** — nothing for stem formation; a printed case grid would be needed before `stem|number` declined groups. First Steps leaves the PPP out of scope (~p.150).

**What I did** — Replaced the thin five-card deck with **33** cards. Groups: `ta|weak` (4), `ta|drop-nasal` (4), `ta|aspirate` (4), `ta|samprasarana` (4), `ta|it` (4), `ta|a-to-i` (4), `ta|c-j` (4), `tavat|vat` (5) — each ≥4 distinct IAST. Lesson cells kept; joins from the `-tvā` sound-change rules this page incorporates, plus Gonda §§102–103 (pp.76–77). Textbook swaps: Gonda √bhṛ→bhṛta; √pat→likhita/carita; √nam→nata; √yudh/tyaj/bhuj for aspirate and c/j peers; √gam/√nī + `-vat` for extra tavat stems. Lesson `śakta` and the sentence `rāmeṇa śaraḥ kṛtaḥ` left out (one-off sense / not a formation group). Devanagari checked with `scripts/rudra-iast.py`.

**Check** — unverified
