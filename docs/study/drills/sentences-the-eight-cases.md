# sentences-the-eight-cases

**Judgment** — This page drills the eight case senses by picking the matching masculine short `-a` form for a given case, number, and stem.

**Outcome** — generate

**What I would need** — nothing

**What I did** — 42 cards on stems `nara`, `siṃha`, and `grāma` (singular and plural only). Groups: `nara|sg`, `nara|pl`, `siṃha|sg`, `siṃha|pl`, `grāma|sg`, `grāma|pl` (each ≥4 distinct IAST). Collapsed shared spellings (pl nom=voc; pl dat=abl). Dual left out: only three distinct spellings after collapse. Cue: First Steps Towards Sanskrit §6.2 case functions (pp.105–108), §6.3 `deva` paradigm (p.121), §6.7.1 (p.131); textbook `aśva`/`deva` swapped to lesson stems `nara`, `siṃha`, `grāma`. Devanagari checked with `scripts/rudra-iast.py`.

**Check** — unverified
