# nominals-ai-o-au-stems

- **Judgment** — This page drills the rare -ai / -o / -au case forms from the printed `rai`, `go`, and `nau` paradigms.
- **Outcome** — `generate`.
- **What I would need** — nothing.
- **What I did** — Replaced the thin five-card deck with **34** cards from printed lesson cells only. Stems: `rai` (sg+pl), `go` (sg+pl), `nau` (sg+pl). Groups: `rai|sg`, `rai|pl`, `go|sg`, `go|pl`, `nau|sg`, `nau|pl` (each ≥4 distinct IAST). Collapsed shared spellings (sg nom=voc; sg abl=gen; pl dat=abl; rai/nau pl nom=acc=voc; go pl nom=voc with separate acc). Dual omitted (only three distinct spellings after collapse). No noun swap — `dyo` is named but not declined in the lesson table. Cue: Gonda §31 p.25 (nau, go); First Steps has no diphthong-stem exercise. Devanagari checked with `scripts/rudra-iast.py`.
- **Check** — `unverified`.
