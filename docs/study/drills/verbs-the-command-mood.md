# verbs-the-command-mood

**Judgment** — This page drills the command-mood (loṭ) person endings on a thematic present stem, from the printed `naya` grid.

**Outcome** — generate

**What I would need** — nothing

**What I did** — Replaced the thin five-card deck with **27** cards. Roots/stems: `nī`/`naya` (lesson grid), `pat`/`pata` and `ji`/`jaya` (same first-class join). Groups: `nī|command`, `pat|command`, `ji|command` (9 distinct IAST each). Cue: Gonda §60 p.48 imperative of `bhṛ`/`bhara`; First Steps mentions the imperative (~p.142) but has no paradigm drill. Textbook `bhara` swapped to `pata`/`jaya`. Ātmanepada left out (lesson note). Devanagari checked with `scripts/rudra-iast.py`.

**Check** — unverified
