# compounds-the-dvandva

**Judgment** — Form an itaretara or samāhāra dvandva from a `ca` paraphrase: number matches the members and gender follows the last (itaretara), or neuter singular (samāhāra).

**Outcome** — generate

**What I would need** — nothing for ordinary stem joins; a closed reshape rule (or a fourth printed irregular) before `mātāpitarau` / `mitrāvaruṇau` / `dyāvāpṛthivī` can be a practice group.

**What I did** — Replaced the thin five-card deck with **20** cards. Groups: `itaretara|dual` (8), `itaretara|plural` (7), `samāhāra` (5) — each ≥4 distinct IAST. Cue: Gonda §109 (p.82); lesson glyphs kept; First Steps has no dvandva drill. Noun swaps: Gonda `hastin`→`gaja` / `aśva` / `nara`, `suta`→`putra`, animal triple → `aśva-gaja-nara`, abstract pair → `śīta-uṣṇa`; lesson `phala-puṣpa` order swap. Reshaped forms left out. Devanagari checked with `scripts/rudra-iast.py`.

**Check** — unverified
