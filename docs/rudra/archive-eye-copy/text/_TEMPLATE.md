# <id>

```meta
id: namakam-01
ts: 4.5.1
pdf: TS 4.5 Sanskrit Pada Paatam with Vaakyam.pdf
pages: 4-9
expected_padams: 222
```

Replace this whole file. Keep the fence names (`meta`, `samhita`, `samhita_iast`, `padas`). One `##` heading per pañcati, matching the PDF label (`4.5.1.1` not `TS 4.5.1.1`). Columns in `padas` are tab-separated. `after` is one of `join` / `space` / `danda` / `ddanda`. See `docs/rudra/ENCODE.md`.

## 4.5.1.1

```samhita

```

```samhita_iast

```

```padas
pada_dev	pada_iast	slice_dev	slice_iast	after
```
