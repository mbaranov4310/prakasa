# Archived eye-copy (not a source)

These files were produced by reading page images and typing Devanagari. That method does not produce a checkable transcription of the VedaVMS PDFs.

Do **not** use this directory as a source for Prakāśa, Stage 1, or the decoder. The decode pipeline in `docs/rudra/decode/` is the working copy.

Optional later: diff against the mechanical decode. Not a gate.
